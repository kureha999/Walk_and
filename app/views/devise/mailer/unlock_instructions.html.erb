<% content_for :title, t('devise.mailer.unlock_instructions.header') %>
<p><%= t('devise.mailer.unlock_instructions.hello') %><%= @resource.email %>!</p>

<p><%= t('devise.mailer.unlock_instructions.account_locked') %></p>

<p><%= t('devise.mailer.unlock_instructions.unlock_instructions') %></p>

<p><%= link_to t('devise.mailer.unlock_instructions.unlock_link'), unlock_url(@resource, unlock_token: @token) %></p>