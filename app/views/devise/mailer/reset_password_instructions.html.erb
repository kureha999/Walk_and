<% content_for :title, t('devise.mailer.reset_password_instructions.header') %>
<p><%= t('devise.mailer.reset_password_instructions.hello') %><%= @resource.name %></p>

<p><%= t('devise.mailer.reset_password_instructions.password_reset_request') %></p>

<p><%= link_to t('devise.mailer.reset_password_instructions.change_password_link'), edit_password_url(@resource, reset_password_token: @token) %></p>

<p><%= t('devise.mailer.reset_password_instructions.ignore_if_not_requested') %></p>
<p><%= t('devise.mailer.reset_password_instructions.password_not_changed') %></p>