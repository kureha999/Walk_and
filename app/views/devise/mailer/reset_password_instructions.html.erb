<p>こんにちは <%= @resource.name %>さん^^!</p>

<p>パスワードのリセットリンクをリクエストしました。以下のリンクをクリックして、パスワードを変更してください。</p>

<p><%= link_to '~パスワードを変更する~', edit_password_url(@resource, reset_password_token: @token) %></p>

<p>リクエストしていない場合こちらのメールを無視して下さい。</p>
<p>上記のリンクにアクセスして新しいパスワードを作成するまで、パスワードは、変更されません。</p>