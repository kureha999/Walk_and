<footer class="bg-footer-color text-text-cream py-4">
  <div class="container mx-auto flex items-center justify-between px-4 max-w-[600px]">
    <!-- 左側: コピーライト -->
    <div class="text-sm font-dancing-script">
      <%= t('shared.footer.copyright') %>
    </div>
    <!-- 右側: リンクボタン -->
    <div class="flex space-x-4 text-sm">
      <%= link_to t('shared.footer.contact'), '#', class: 'hover:underline' %>
      <%= link_to t('shared.footer.terms'), '#', class: 'hover:underline' %>
      <%= link_to t('shared.footer.privacy_policy'), '#', class: 'hover:underline' %>
    </div>
  </div>
</footer>